import 'package:flutter/material.dart';

abstract class FretRadius {
  static const defaultBorderRadius = BorderRadius.all(Radius.circular(6));
  static const buttonBorderRadius = BorderRadius.all(Radius.circular(24));
}